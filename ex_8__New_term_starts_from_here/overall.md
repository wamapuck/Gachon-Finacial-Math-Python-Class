# 🧠 프로그래밍 기초 정리 노트  
> **정리 기준: PDF 업로드 역순 (📄 stdlib → dictionary → set/tuple → string formatting → class)**  
> **목표: 개념 설명 + 코드 예시 병행**

---

## 📘 1. Python 표준 라이브러리 (`ProgBasic1_stdlib.pdf`)

Python은 다양한 기능을 손쉽게 사용할 수 있도록 기본적으로 여러 **표준 라이브러리**를 제공합니다. 이 라이브러리를 통해 복잡한 작업(파일 처리, 수학 계산, 웹 접근 등)을 간단한 코드로 처리할 수 있습니다. 대표적으로 `math`, `os`, `random`, `glob`, `urllib` 등이 있습니다.

---

### 🔹 정렬 함수

리스트 정렬에는 원본을 직접 정렬하는 방식과, 정렬된 사본을 만드는 방식이 있습니다.

| 함수       | 설명                     | 원본 유지 |
|------------|--------------------------|------------|
| `sort()`   | 리스트 자체를 정렬       | ❌         |
| `sorted()` | 정렬된 새 리스트 반환    | ✅         |

```python
a = [3, 1, 2]
a.sort()  # a 자체가 정렬됨

a = [3, 1, 2]
b = sorted(a)  # b는 정렬된 복사본
```

---

### 🔹 주요 모듈

#### 🔸 `math`
수학 관련 기능 제공. 삼각함수, 로그, 상수 등을 포함합니다.

```python
import math
math.pi             # 3.141592...
math.cos(math.pi/4) # 0.7071
math.log(1024, 2)   # 10.0
```

#### 🔸 `os`
운영체제의 파일 시스템과 상호작용 가능. 현재 디렉토리 변경, 파일명 변경 등.

```python
import os
os.getcwd()
os.chdir("path")
os.rename("a.txt", "b.txt")
os.remove("file.txt")
```

#### 🔸 `glob`
파일명 패턴 검색. 특정 확장자의 파일 목록을 가져올 때 사용.

```python
import glob
glob.glob("*.txt")
```

#### 🔸 `random`
무작위 수, 무작위 선택 등에 사용. 시뮬레이션이나 게임에 활용 가능.

```python
import random
random.choice([1, 2, 3])
random.sample(range(10), 3)
random.random()
random.randrange(6)
```

#### 🔸 `urllib`
웹에서 데이터를 읽어올 수 있게 해주는 모듈입니다. API나 HTML 파싱 등에 활용됩니다.

```python
import urllib.request
data = urllib.request.urlopen("http://example.com").read().decode("utf-8")
```

---

## 📘 2. Dictionary (`ProgBasic1_dictionary_1.pdf`)

**딕셔너리(Dictionary)**는 키(key)와 값(value) 쌍으로 데이터를 저장하는 자료구조입니다. 데이터 간의 명확한 매핑이 필요할 때 사용하며, 조회 속도가 빠르고 다양한 메서드를 통해 데이터를 효과적으로 조작할 수 있습니다.

---

### 🔹 기본 사용법

```python
grades = {"John": "A", "Anna": "B+"}
grades["New"] = "C"
```

---

### 🔹 주요 메서드

| 메서드              | 설명                              |
|---------------------|-----------------------------------|
| `del d[key]`        | 특정 키 제거                      |
| `d.keys()`          | 모든 key 반환                     |
| `d.values()`        | 모든 value 반환                   |
| `d.items()`         | (key, value) 튜플 리스트 반환     |
| `d.get(key, def)`   | key 없을 경우 default 반환        |
| `d.clear()`         | 전체 딕셔너리 삭제                |

딕셔너리를 조회할 때 `key`가 존재하지 않으면 에러가 발생하므로, 안전하게 접근하려면 `get()`이나 `"in"` 연산자를 사용합니다.

```python
if 'apple' in basket:
    print(basket['apple'])

basket.get('apple', '없음')  # 안전한 접근
```

---

### 🔹 `setdefault()` 와 `defaultdict`

- `setdefault()`는 특정 key가 존재하지 않으면 값을 초기화 후 반환
- `defaultdict`는 없는 key에 대해 자동으로 초기값을 설정

```python
d = {}
d.setdefault("bob", 0)

from collections import defaultdict
counter = defaultdict(int)
counter['apple'] += 1
```

---

## 📘 3. Set & Tuple (`ProgBasic1_set_tuple.pdf`)

파이썬에는 기본적인 데이터 집합을 표현하기 위해 `set`과 `tuple`이 존재합니다. 이들은 모두 리스트와는 다른 특성과 용도를 가집니다.

---

### 🔹 Set (집합)

Set은 **중복을 허용하지 않으며**, 요소 간의 **순서가 보장되지 않습니다.** 데이터의 유일성 보장이 필요할 때 적합합니다.

```python
s = set()
s.add(1)
s.add(2)
print(2 in s)
```

---

### 🔹 Tuple (튜플)

Tuple은 리스트처럼 순서가 있지만 **수정이 불가능한(immutable)** 자료형입니다. 안전하게 데이터를 유지하고자 할 때 사용됩니다.

```python
t = (1, 2, 3)
print(t[0])
```

튜플은 대입 및 루프, 딕셔너리와 함께 사용할 때 유용합니다.

```python
(x, y) = (4, "fred")
for (k, v) in {"a":1, "b":2}.items():
    print(k, v)
```

---

## 📘 4. 문자열 포맷팅 (`ProgBasic1_string_formatting.pdf`)

문자열 내에 데이터를 삽입하는 방식은 매우 다양하며, 상황에 따라 `%`, `format()`, `f-string`을 선택하여 사용할 수 있습니다.

---

### 🔹 `%` 포맷팅

고전적인 방식으로, 포맷 코드를 사용해 데이터를 삽입합니다.

```python
"I ate %d apples." % 3
"I ate %s apples." % "five"
```

| 포맷 코드 | 의미       |
|-----------|------------|
| `%s`      | 문자열     |
| `%d`      | 정수       |
| `%f`      | 실수       |
| `%%`      | % 문자 자체 |

---

### 🔹 `format()` 메서드

가독성이 높은 포맷 방식. 인덱스 혹은 키워드 기반 지정 가능.

```python
"I ate {} apples".format(3)
"I ate {1} apples for {0} days".format("two", 3)
"I ate {num} apples".format(num=3)
```

---

### 🔹 f-string

Python 3.6부터 지원되며, 가장 직관적이고 가독성이 높은 방식입니다.

```python
a = 3
f"I ate {a} apples"
f"{'hi':>10}"    # 오른쪽 정렬
f"{3.14159:.2f}" # 소수점 2자리
```

중괄호 내부에 수식도 사용할 수 있어 유연합니다.

```python
f"List length: {len([1,2,3])}"
```

---

## 📘 5. 클래스 & 객체지향 프로그래밍 (`ProgBasic1_class.pdf`)

Python은 객체지향 프로그래밍(OOP)을 지원합니다. **클래스(class)**는 객체(object)를 생성하기 위한 설계도이며, 각 객체는 해당 클래스의 인스턴스(instance)입니다.

---

### 🔹 클래스 정의 및 생성자

`__init__()`은 객체가 생성될 때 자동으로 호출되는 생성자입니다.

```python
class Athlete:
    def __init__(self, weight=60):
        self.weight = weight
    def how_big(self):
        return self.weight
```

```python
a = Athlete(80)
print(a.how_big())  # 80
```

---

### 🔹 상속과 메서드 확장

상속을 통해 기존 클래스를 확장하여 새로운 기능을 구현할 수 있습니다.

```python
class Runner(Athlete):
    def __init__(self, speed):
        super().__init__()
        self.speed = speed
    def how_fast(self):
        return self.speed
```

---

### 🔹 예제: Counter 클래스 계층 구조

```python
class Counter:
    def __init__(self):
        self.count = 0
    def incr(self):
        self.count += 1
    def reset(self):
        self.count = 0

class Counter5(Counter):
    def incr5(self):
        self.count += 5

class CounterR(Counter):
    def reset(self):
        self.count = -1
```